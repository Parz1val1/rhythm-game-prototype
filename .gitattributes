# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf

# Preserve pinned third-party and generated Wwise spacing without hand edits.
/addons/Wwise/** -whitespace
/WwiseBanks/**/*.txt -whitespace
